# Agent Foreman Configuration
# Copy this file to .env and customize as needed
# Can also be placed at ~/.agent-foreman.env for global settings

# ═══════════════════════════════════════════════════════════════
# AGENT CONFIGURATION
# ═══════════════════════════════════════════════════════════════

# Configure which AI agents to use and their priority order
# - Order determines priority: first agent is tried first
# - Only listed agents are enabled (others are ignored even if installed)
# - Valid agent names: claude, codex, gemini
# - Default: codex,gemini,claude
#
# Examples:
#   AGENT_FOREMAN_AGENTS=claude              # Only use Claude
#   AGENT_FOREMAN_AGENTS=claude,gemini       # Prefer Claude, Gemini as fallback
#   AGENT_FOREMAN_AGENTS=codex,gemini,claude # Default order (explicit)
#
# AGENT_FOREMAN_AGENTS=codex,gemini,claude

# ═══════════════════════════════════════════════════════════════
# TIMEOUT CONFIGURATION
# ═══════════════════════════════════════════════════════════════
#
# All timeout values are in milliseconds
#
# Default values are designed to cover 95%+ of projects:
# - Small projects (< 10k LOC): typically complete in seconds
# - Medium projects (10k-100k LOC): typically complete in 1-3 minutes
# - Large projects (100k+ LOC, monorepos): may need up to 10 minutes

# Project survey/scan - AI explores entire codebase (default: 600000 = 10 minutes)
# This is the most intensive operation. Large monorepos may need the full 10 minutes.
# AGENT_FOREMAN_TIMEOUT_SCAN=600000

# Generate features from existing survey document (default: 180000 = 3 minutes)
# Text-to-JSON conversion, typically fast (< 1 min)
# AGENT_FOREMAN_TIMEOUT_SURVEY=180000

# Generate features from goal description (default: 180000 = 3 minutes)
# Text-to-JSON conversion, typically fast (< 1 min)
# AGENT_FOREMAN_TIMEOUT_GOAL=180000

# Merge init.sh script with AI (default: 120000 = 2 minutes)
# Simple text merge, very fast
# AGENT_FOREMAN_TIMEOUT_MERGE_INIT=120000

# Merge CLAUDE.md with AI (default: 120000 = 2 minutes)
# Simple text merge, very fast
# AGENT_FOREMAN_TIMEOUT_MERGE_CLAUDE=120000

# AI verification of feature completion (default: 300000 = 5 minutes)
# Includes running tests/builds + AI analysis
# AGENT_FOREMAN_TIMEOUT_VERIFY=300000

# AI capability discovery (default: 120000 = 2 minutes)
# Analyzes project structure to detect test/build commands
# AGENT_FOREMAN_TIMEOUT_CAPABILITY=120000

# Global default for any AI agent call (default: 300000 = 5 minutes)
# Used as fallback when no specific timeout is configured
# AGENT_FOREMAN_TIMEOUT_DEFAULT=300000
