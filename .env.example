# Agent Foreman Configuration
# Copy this file to .env and customize as needed
# Can also be placed at ~/.agent-foreman.env for global settings

# ═══════════════════════════════════════════════════════════════
# AGENT CONFIGURATION
# ═══════════════════════════════════════════════════════════════

# Configure which AI agents to use and their priority order
# - Order determines priority: first agent is tried first
# - Only listed agents are enabled (others are ignored even if installed)
# - Valid agent names: claude, codex, gemini
# - Default: codex,gemini,claude
#
# Examples:
#   AGENT_FOREMAN_AGENTS=claude              # Only use Claude
#   AGENT_FOREMAN_AGENTS=claude,gemini       # Prefer Claude, Gemini as fallback
#   AGENT_FOREMAN_AGENTS=codex,gemini,claude # Default order (explicit)
#
# AGENT_FOREMAN_AGENTS=codex,gemini,claude

# ═══════════════════════════════════════════════════════════════
# TIMEOUT CONFIGURATION
# ═══════════════════════════════════════════════════════════════
#
# All timeout values are in milliseconds
#
# Default values are designed to cover 95%+ of projects:
# - Small projects (< 10k LOC): typically complete in seconds
# - Medium projects (10k-100k LOC): typically complete in 1-3 minutes
# - Large projects (100k+ LOC, monorepos): may need up to 15 minutes

# Project survey/scan - AI explores entire codebase (default: 900000 = 15 minutes)
# This is the most intensive operation. Large monorepos may need the full 15 minutes.
# AGENT_FOREMAN_TIMEOUT_SCAN=900000

# Generate features from existing analyze document (default: 300000 = 5 minutes)
# Text-to-JSON conversion, may take 2-3 minutes for large analysis
# AGENT_FOREMAN_TIMEOUT_ANALYZE=300000

# Generate features from goal description (default: 300000 = 5 minutes)
# Text-to-JSON conversion, typically faster than analysis parsing
# AGENT_FOREMAN_TIMEOUT_GOAL=300000

# Merge init.sh script with AI (default: 300000 = 5 minutes)
# Script merging can take 2-3 minutes for complex customizations
# AGENT_FOREMAN_TIMEOUT_MERGE_INIT=300000

# Merge CLAUDE.md with AI (default: 300000 = 5 minutes)
# Document merging can take 3-4 minutes for large CLAUDE.md files
# AGENT_FOREMAN_TIMEOUT_MERGE_CLAUDE=300000

# AI verification of feature completion (default: 600000 = 10 minutes)
# Includes running tests/builds + AI analysis
# AGENT_FOREMAN_TIMEOUT_VERIFY=600000

# AI capability discovery (default: 300000 = 5 minutes)
# Analyzes project structure to detect test/build commands
# AGENT_FOREMAN_TIMEOUT_CAPABILITY=300000

# Global default for any AI agent call (default: 600000 = 10 minutes)
# Used as fallback when no specific timeout is configured
# AGENT_FOREMAN_TIMEOUT_DEFAULT=600000
